#This file allows us to store our Terraform state files in a secure location, namely
#an Azure storage account, as denoted below
#Our ADO build pipeline will leverage this entirely, and pass in specific settings at runtime
#as if we ran it from our VSCode via "terraform init", which is why its an empty codeblock
terraform {
  backend "azurerm" {}
}

# Dev environment preview (what's done at the pipeline)
#NOTE: Required information includes storage account name, container name, and key which we've
#already defined as injections in the ADO pipline's YAML code. We just show that part below here:

#terraform init \
#  -backend-config=backend.conf \
#  -backend-config="resource_group_name=dev-rg" \
#  -backend-config="storage_account_name=tfstate-storage" \
#  -backend-config="container_name=tfstate" \
#  -backend-config="key=$(environment).tfstate"
